# catalog-info.yaml
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: minikube-cluster-view
  annotations:
    backstage.io/kubernetes-id: minikube
spec:
  type: service
  owner: user:guest
  lifecycle: experimental
